# 多 AI 协作 · 最小可行方案（v2 精简版）

> v1 调研版见 git 历史。本版原则：**1 个主 AI 起步，够用就不加人。**

## 澄清：11 角色 ≠ 11 个 AI

- 我（opencode）一个会话里可以戴多顶帽子（写代码/写脚本/管集成），派 subagent 也是我，不占额外额度
- Mixamo、Meshy、Qwen-Image、Suno 是**生成工具**，不是需要协调的 AI 同事
- 真正需要“协调”的只有会写文件、会改代码的 AI

## 三档配置（按需升级，别预支复杂度）

| 档位 | 配置 | 什么时候用 |
|---|---|---|
| **A（零依赖）** | 只有我。代码+脚本+集成全包，杂活也我干 | 现在就能跑 |
| **B（推荐）** | 我 + DeepSeek 一个辅助 | 想让草稿活并行时（见下） |
| **C（量大了）** | B + 一个审查 AI（OmniRoute 路由 Claude/GPT） | 单时代码 >800 行、或审查成瓶颈时 |

你已有 DeepSeek → **直接上 B 档，这就是满配**。

## B 档分工（总共 2 个 AI + 工具线）

| 谁 | 干什么 | 不干什么 |
|---|---|---|
| **我（opencode 主会话）** | orchestrator：契约、全部游戏代码、Blender 脚本、集成、smoke、git、tasks 看板 | — |
| **DeepSeek** | 只做**并行草稿**：关卡配置表、本地化文案、广告文案、数值平衡表、测试用例。产出扔进 `docs/drafts/`，我审查后才合并 | 不碰游戏代码，不直接改工程 |
| **工具线**（不是 AI 同事） | Meshy/Tripo 出模型、Mixamo 绑动画、Qwen-Image/Seedream 出图、Suno 出音乐 | 全部经我之手进包 |

**DeepSeek 怎么接**：OmniRoute 网关（`http://127.0.0.1:20128/v1`）或直连 API，我写个脚本把“生成 30 关配置”这类任务批量丢给它，回来我验。没有它也完全能跑——A 档就是只有我。

## 精简协调机制（2 文件 2 门禁）

**文件（从 6 个砍到 2 个）**：
- `assets/scripts/config.ts` —— 唯一数值源，谁都不能在别处复制数值
- `docs/tasks.md` —— 任务看板 + DeepSeek 草稿收货区 + 资产记录，一个文件全管

**门禁（从 5 个砍到 2 个）**：
- **G1 手感门禁**：你连玩 30 分钟还想再来 = 立项（不可外包给 AI）
- **G3 代码门禁**：`node tools/smoke.ts` 绿 + 我自查（大改才送第二个 AI 审查）

**铁律不变**：AI 之间不直接对话，一切文件交接；跨域改动（代码↔资产）只能我经手；不可逆操作（删文件/花钱批量生成）你确认。

## 扩容信号（什么时候才加 AI）

出现以下任一条再加，不预支：
1. 单次要写的代码超过 800 行 → 拆给 subagent 并行（还是我）
2. 我审查自己代码开始看走眼 → 上 C 档审查 AI
3. 资产要批量产 50+ 个 → 写批处理脚本（工具，不是 AI）

## 一句话

> **一个主 AI + 一个草稿 AI + 一条工具线，两个文件、两个门禁。** 多人协作是病，单人会集成才是药。
