# Shortcut Run 复刻 · 灰模原型 v0.1

玩法闭环已用纯逻辑实现并通过测试，场景全部由代码用同一个灰盒 prefab 搭出来。
**目标只有一个：验证手感，不好看没关系。**

## 1. 环境

- Cocos Creator **3.8.x**（Cocos 官网下载，Dashboard 安装）
- 微信开发者工具暂时不用（等接广告/上架阶段）

## 2. 打开项目

Cocos Dashboard → **导入**（或"打开其他项目"）→ 选择本目录 `shortcut-run`。

> 如果 Dashboard 不认这个目录：新建一个空的 **3D** 项目，把本项目的 `assets/` 文件夹整个覆盖进去即可。

## 3. 做灰盒 prefab（唯一的美术步骤，约 1 分钟）

1. 层级管理器空白处右键 → **创建 → 3D 对象 → Box**
2. 把这个 Box 节点从"层级"拖到"资源管理器"面板 → 生成 `box.prefab`
3. 把场景里的 Box 删掉（prefab 已留下）

## 4. 装配场景（约 2 分钟）

1. 资源管理器右键 **创建 → Scene**，命名 `game`，双击打开
2. 场景根节点建一个空节点（右键 → **创建空节点**），命名为 `Bootstrap`
3. `Bootstrap` 挂 **GameApp** 组件（属性检查器 → 添加组件 → 用户脚本 → GameApp）
4. 把 `box.prefab` 拖到 GameApp 的 **BoxPrefab** 字段
5. 相机：新建 3D 场景默认带 `Main Camera`；如果没有，右键创建 Camera 节点并命名为 `Main Camera`（脚本会自动挂跟随逻辑）

### 4.5 HUD（可选，约 3 分钟）

不配也能玩（砖块数看控制台），但试玩时建议配上：

1. 层级右键 → **创建 → UI → Label**（引擎会自动生成 `Canvas` 和 UI 相机），命名为 `BrickLabel`
   - 位置拖到屏幕上方中间；字号调大（如 60）；颜色白色
2. 再建一个 Label 命名为 `HintLabel`，放屏幕中央
3. `Canvas` 节点挂 **GameUI** 组件，把两个 Label 分别拖到 `BrickLabel` / `HintLabel` 字段

效果：上方常驻"砖块 N"，中央显示开始提示 / 胜利 / 失败原因。

## 5. 运行

编辑器顶部按 **▶** 预览。

| 操作 | 按键 |
|---|---|
| 开始 | 任意点击 / 任意键 |
| 转向 | 鼠标左键拖动，或 `A`/`D`、`←`/`→` |
| 重开 | 胜利/失败 1.6 秒后自动重开 |

规则：自动前进 → 吃砖块（身后拖砖堆 = 携带量）→ 断崖处砖够自动拍桥下来、砖不够掉落 → 终点门验砖，够就开门胜利。配了 HUD 的话上方显示砖块数、中央显示状态提示。

## 6. 调参（手感不对就改这里）

全部在 `assets/scripts/config.ts`，先调这三个：

| 参数 | 作用 | 调整方向 |
|---|---|---|
| `runSpeed` / `speedPerBrick` / `maxSpeed` | 速度与加速感 | 觉得肉就先加 20% |
| `steerSpeed` / `steerPerPixel` | 转向跟手度 | 觉得涩就加大 |
| `gapWidthMin/Max`、`gapIntervalMin/Max` | 难度曲线 | 太难就缩窄断崖/拉大间隔 |

`seed`（GameApp 组件上）换数字 = 换一张关卡图。

## 7. 决策关口

**连玩 30 分钟。** 还想再来一把 = 立项成功，进入 v0.2（HUD/音效/Mixamo 角色/广告 SDK/主题换肤）；觉得无聊 = 趁早砍，只亏两周。

## 8. 测试

```bash
node tools/smoke.ts   # 关卡生成器 50 种子不变量测试
```
